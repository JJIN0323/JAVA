package com.ohgriffers.practice.array;

public class Application01 {

        public static void main(String[] args) {

            /* Q1.길이가 10인 정수형 배열을 선언 및 할당한 뒤 각 인덱스에 차례대로 1부터 10까지 값을 넣고 출력하세요.
             * */

            // int [] iarr (주소) = new int[5];
            // sout (iarr) : [I@sdifjsidfs323123] 주소가 나온다. -> 레퍼런스 변수 !
            // sout (iarr[1] : 4 <- 원하는 값을 출력 가능.
            // iarr = new int[6]; 이라고 하면, 새로운 공간을 다시 창출한다.
            // iarr = null -> 이건 에러 발생.

            // array ppt -> 7p 참고! array만 만들어도 기본값을 다 채워준다.

            // 규칙이 있어서 반복문을 사용 할 수 있다.

            // int [] arr = new int [5]
            // int [] [] iarr2 = new int [] []; <- 다차원 배열 (쓸일 없음)


            // 배열의 복사 : 얕은 복사와 깊은 복사가 있다.


        }
    }
